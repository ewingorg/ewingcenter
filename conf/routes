# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                       Application.index

GET     /error                               Application.error

# Ignore favicon requests
GET     /favicon.ico                            404

*  /product/indexList    WebResourceController.queryIndexProduct

